<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.met.workout">

    <dist:module
        dist:instant="false"
        dist:title="@string/title_workout">
        <dist:delivery>
            <dist:install-time />
        </dist:delivery>

        <dist:fusing dist:include="true" />
    </dist:module>

    <application>
        <activity android:name=".own_plan.SearchExerciseActivity"></activity>
        <activity
            android:name=".own_plan.MyOwnPlanActivity"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>